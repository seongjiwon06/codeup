1076 : [기초-반복실행구조] 문자 한 개 입력받아 알파벳 출력하기 

#include<stdio.h>
int main()
{
	char ch;//문자는  아스키 코드를 가지고 있다(= 문자는 두가지로 사용 가능하다) 
	int i;
	scanf("%c",&ch);//문자 1개 입력	
	
	for(i='a';i<=ch;i++)//정수는 그냥/문자는 작은따옴표/문자영은 큰따옴표 
	{
		printf("%c ",i);
	}//문자를 아스키코드로 변환(정수), 아스키코드를(정수)를 문자로 변환 
}